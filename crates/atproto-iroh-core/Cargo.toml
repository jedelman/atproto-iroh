[package]
name = "atproto-iroh-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Stub crate — see ../../SPEC.md and ../../CLAUDE.md before adding real code here."

# Dev-only: the `iroh_docs_probe` example under examples/ is the throwaway
# experiment CLAUDE.md calls for — validating §6's iroh-docs assumptions
# against the real crate before anything here becomes load-bearing. Not
# used by the (currently empty) library itself.
[dev-dependencies]
iroh = "1.2.0"
iroh-docs = "0.101.0"
iroh-blobs = "0.103.0"
iroh-gossip = "0.101.0"
tokio = { version = "1.53.1", features = ["full"] }
anyhow = "1"
n0-future = "0.3"
